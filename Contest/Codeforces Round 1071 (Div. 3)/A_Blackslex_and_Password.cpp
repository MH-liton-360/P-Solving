#include <bits/stdc++.h>
using namespace std;

int main()
{

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--)
    {
        /* code */
        int k, x;
        cin >> k >> x;

        int n = (x * k) + 1;
        cout << n << "\n";
    }

    return 0;
}